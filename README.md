# Encryptcon

For Windows

python -m venv .
Scripts\activate
pip install -U Flask Authlib requests
python -m pip install "pymongo[srv]"

For Linux and MacOS

python3 -m venv venv
source venv\bin\activate
pip install -U Flask Authlib requests
python -m pip install "pymongo[srv]"